name: spend_deploy_prod
on:
  push:
    branches: 
      - master
    paths:
      - 'niffler-spend/**'
      
jobs:
  checkout:
    runs-on: prod-runner  
    steps:
      - uses: actions/checkout@v3

  deploy:
    runs-on: prod-runner
    needs: checkout
    steps:  
      - name: deploy
        working-directory: ./
        run: |
         pwd
         bash ./docker-compose-dev.sh
         docker-compose up -d --force-recreate niffler-spend
         docker system prune  -a -f
         echo "done"
      - name: push
        run: |
         docker login --username ${{ vars.DOCKER_HUB_ACC }} --password "${{ secrets.DOCKER_HUB_PASSWORD }}"
         docker tag dtuchs/niffler-spend:latest ${{ vars.DOCKER_HUB_ACC }}/niffler-spend:${{github.run_number}}
         docker push ${{ vars.DOCKER_HUB_ACC }}/niffler-spend:${{github.run_number}}
