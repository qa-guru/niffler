name: gateway_deploy_prod
on:
  push:
    branches: 
      - master
    paths:
      - 'niffler-gateway/**'
      
jobs:
  checkout:
    runs-on: prod-runner  
    steps:
      - uses: actions/checkout@v3

  deploy:
    runs-on: prod-runner
    needs: checkout
    steps:  
      - name: deploy
        working-directory: ./
        run: |
         pwd
         bash ./docker-compose-dev.sh
         docker-compose up -d --force-recreate niffler-gateway
         docker system prune  -a -f
         echo "done"
