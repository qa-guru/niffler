name: currency_deploy_stage
on:
  push:
    branches: 
      - master
    paths:
      - 'niffler-currency/**'
      
jobs:
  checkout:
    runs-on: prod-runner  
    steps:
      - uses: actions/checkout@v3

  deploy:
    runs-on: prod-runner
    needs: checkout
    steps:  
      - name: deploy
        working-directory: ./
        run: |
         pwd
         bash ./docker-compose-dev.sh
         docker-compose up -d --force-recreate niffler-currency
         docker system prune  -a -f
         echo "done"
