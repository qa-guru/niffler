mutation CreateSpend($input: SpendInput!) {
    spend(input: $input) {
        id
        spendDate
        currency
        amount
        description
        category {
            id
            name
            username
            archived
        }
        username
    }
}
