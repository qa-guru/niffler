query AllPeople {
    allPeople(page: 0, size: 10) {
        edges {
            node {
                id
                username
                friendshipStatus
            }
        }
        pageInfo {
            hasPreviousPage
            hasNextPage
        }
    }
}